apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-dev
  namespace: dev-env
spec:
  replicas: 5
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-app
        image: nginx:latest # 개발 환경은 최신 버전을 씁니다!
        ports:
        - containerPort: 80
